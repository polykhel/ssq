Super Simple Quizzer
----------------------------
Super Simple Quizzer (in-progress)
- ssq-core
- ssq-bom

To use this project:
- run `./mvnw clean install -Dgpg.skip=true`, on Windows run `.\mvnw.cmd clean install -D"gpg.skip=true"`